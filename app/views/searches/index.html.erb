<div class='content'>
    <h2>Search Results:</h2>
    <div>
      <% if @users.blank? %>
        <p>No results found</p>
      <% else %>
        <% @users.each do |user| %>
          <p><%= link_to user.name, user_path(user.id) %></p>
        </h3>
        <% end %>
      <% end %>
    </div>
</div>