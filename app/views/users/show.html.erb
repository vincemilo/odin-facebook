<div class='content'  >
    <h2><%= @friend.first_name + ' ' + @friend.last_name %></h2>
    <% if @friendship.blank? %>
      <%= button_to 'Add Friend', user_friendships_path(1) %>
    <% else %>
      <h4>You are friends</h4>
    <% end %>
</div>