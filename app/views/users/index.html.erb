<div class='content'>
    <h1>Friends List</h1>
    <ul>
        <% @friends.each do |friend| %>
            <li>
            <%= link_to friend.name, friend %>
            </li>
        <% end %>
    </ul>
</div>